// Set someAdjective to a string of at least 3 characters and append it to myStr using the += operator.
var someAdjective = "colest and the";
var myStr = "Learning to code is ";
var result = myStr + someAdjective;

console.log(result)
